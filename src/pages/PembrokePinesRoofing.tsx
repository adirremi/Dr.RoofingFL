import LocationPage from './LocationPage';
import { locations } from '../data/locations';

export default function PembrokePinesRoofing() {
  return <LocationPage location={locations.pembrokePines} />;
}
